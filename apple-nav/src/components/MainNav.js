import React from 'react';
import { subNav } from '../Data';

const MainNav = (props) => {
    console.log('this is main nav props',props);
    return(
        <div>TEST</div>
    )
} 

export default MainNav;